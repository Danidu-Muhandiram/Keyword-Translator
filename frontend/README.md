# Keyword Translate Frontend

## Setup

1. Install dependencies:
   ```powershell
   npm install
   ```
2. Start development server:
   ```powershell
   npm run dev
   ```

Open the local URL shown in the terminal to view the app.
